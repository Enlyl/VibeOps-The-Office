# Churn Model Documentation

This model predicts user churn based on age and income.
Target variable is `churn`. Note that `dirty_data.csv` contains missing values which must be handled before training.
